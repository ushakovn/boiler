// Code generated by Boiler; DO NOT EDIT.

package storage

import (
  {{- range .ModelMethodsPackages}}
  {{.ImportAlias}} "{{.ImportLine}}"
  {{- end}}
)

func (s *Storage) CreateRocketLock(ctx context.Context, params models.CreateRocketLockParams) (*models.RocketLock, error) {
  query := `INSERT INTO rocket_locks(lock_id, locked_until)
            VALUES (%s, NOW() + INTERVAL %s)
            ON CONFLICT (lock_id)
            DO UPDATE SET locked_until = NOW() + INTERVAL %[2]s
            WHERE rocket_locks.locked_until < NOW()
            RETURNING lock_id, locked_until`

  builder := sql.NewExpr(query, params.LockID, params.LockTTL)

  model, err := pg.Get[*models.RocketLock](ctx, s.executor.Querier(ctx), builder)
  if err != nil {
    if errors.Is(err, pe.ErrModelNotFound) {
      return nil, models.ErrRocketLockConflict
    }
    return nil, err
  }

  return model, nil
}

func (s *Storage) DeleteRocketLock(ctx context.Context, params models.DeleteRocketLockParams) (*models.RocketLock, error) {
  query := `DELETE FROM rocket_locks
            WHERE lock_id = %s
            RETURNING lock_id, locked_until`

  builder := sql.NewExpr(query, params.LockID)

  model, err := pg.Get[*models.RocketLock](ctx, s.executor.Querier(ctx), builder)
  if err != nil {
    if errors.Is(err, pe.ErrModelNotFound) {
      return nil, models.ErrRocketLockNotFound
    }
    return nil, err
  }

  return model, nil
}

func (s *Storage) WithRocketLock(ctx context.Context, params models.WithRocketLockParams, fn func(ctx context.Context) error) error {
  if _, err := s.CreateRocketLock(ctx, models.CreateRocketLockParams{
    LockID:  params.LockID,
    LockTTL: params.LockTTL,
  }); err != nil {
    return fmt.Errorf("s.CreateLock: %w", err)
  }

  defer func() {
    if _, err := s.DeleteRocketLock(ctx, models.DeleteRocketLockParams{
      LockID: params.LockID,
    }); err != nil {
      log.Errorf("rocketLocksStorage: WithLock: s.DeleteLock: %v", err)
    }
  }()

  return fn(ctx)
}
