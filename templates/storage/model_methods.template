// Code generated by Boiler; DO NOT EDIT. {{$modelName := .ModelName}}

package storage

import (
  {{- range .ModelMethodsPackages}}
  {{.ImportAlias}} "{{.ImportLine}}"
  {{- end}}
)

func (s *Storage) List{{$modelName}}(ctx context.Context, params models.List{{$modelName}}Params) ([]*models.{{$modelName}}, error) {
  builder := sql.NewSelect().
    Columns(
      {{- range .ModelFields}}
      string(models.{{.FieldName}}_{{$modelName}}_Field),
      {{- end}}
    ).
    From(string(models.{{$modelName}}_TableName))

  if err := params.Pagination.Validate(); err != nil {
    return nil, fmt.Errorf("pagination.Validate: %w", err)
  }

  offset, limit := params.Pagination.OrDefault().ToOffsetLimit()
  builder = builder.Offset(offset).Limit(limit)

  if params.Sort != nil {
    builder = builder.OrderBy(params.Sort.{{$modelName}}Sort())
  }

  if filters := params.Filters; filters != nil {
    {{- range $modelField := .ModelFields}}
    {{- range .ModelsFieldFilters}}
    if {{.FilterIfStmt}} {
      builder = builder.Where(sq.{{.FilterSqOperator}}{string(models.{{$modelField.FieldName}}_{{$modelName}}_Field): filters.{{.FilterName}}{{.FilterTypeSuffix}}})
    }
    {{- end}}
    {{- end}}
  }

  return pg.Select[*models.{{$modelName}}](ctx, s.executor.Querier(ctx), builder)
}

func (s *Storage) {{$modelName}}(ctx context.Context, params models.{{$modelName}}Params) (*models.{{$modelName}}, error) {
  builder := sql.NewSelect().
    Columns(
      {{- range .ModelFields}}
      string(models.{{.FieldName}}_{{$modelName}}_Field),
      {{- end}}
    ).
    From(string(models.{{$modelName}}_TableName))

  if filters := params.Filters; filters != nil {
    {{- range $modelField := .ModelFields}}
    {{- range .ModelFieldFilters}}
    if {{.FilterIfStmt}} {
      builder = builder.Where(sq.Eq{string(models.{{$modelField.FieldName}}_{{$modelName}}_Field): filters.{{.FilterName}}{{.FilterTypeSuffix}}})
    }
    {{- end}}
    {{- end}}
  }

  model, err := pg.Get[*models.{{$modelName}}](ctx, s.executor.Querier(ctx), builder)
  if err != nil {
    return nil, err
  }
  return model, nil
}

func (s *Storage) Create{{$modelName}}(ctx context.Context, params models.Create{{$modelName}}Params) (*models.{{$modelName}}, error) {
  builder := sql.NewInsert().
    Into(string(models.{{$modelName}}_TableName))

  fields := map[string]any{
    {{- range .ModelFields}}
    {{- if eq .NotNullField true}}
    {{- if eq .WithDefaultField false}}
    string(models.{{.FieldName}}_{{$modelName}}_Field): params.{{.FieldName}},
    {{- end}}
    {{- end}}
    {{- end}}
  }

  {{- range $modelField := .ModelFields}}
  {{- if eq .NotNullField false}}
  {{- if eq .WithDefaultField false}}
  if {{.FieldIfStmt}} {
    fields[string(models.{{$modelField.FieldName}}_{{$modelName}}_Field)] = params.{{.FieldName}}{{.FieldTypeSuffix}}
  }
  {{- end}}
  {{- end}}
  {{- end}}

  builder = builder.SetMap(fields).
    Suffix(models.SuffixReturning)

  model, err := pg.Get[*models.{{$modelName}}](ctx, s.executor.Querier(ctx), builder)
  if err != nil {
    return nil, err
  }
  return model, nil
}

func (s *Storage) Update{{$modelName}}(ctx context.Context, params models.Update{{$modelName}}Params) (*models.{{$modelName}}, error) {
  builder := sql.NewUpdate().
    Table(string(models.{{$modelName}}_TableName))

  fields := map[string]any{}

  {{range .ModelFields}}
  {{- if ne .FieldBadge "pk"}}
  if {{.FieldZeroTypeIfStmt}} {
    fields[string(models.{{.FieldName}}_{{$modelName}}_Field)] = params.{{.FieldName}}{{.FieldZeroTypeSuffix}}
  }
  {{- end}}
  {{- end}}

  builder = builder.
    SetMap(fields).
    {{- range .ModelFields}}
    {{- if eq .FieldBadge "pk"}}
    Where(sq.Eq{string(models.{{.FieldName}}_{{$modelName}}_Field): params.{{.FieldName}}}).
    {{- end}}
    {{- end}}
    Suffix(models.SuffixReturning)

  model, err := pg.Get[*models.{{$modelName}}](ctx, s.executor.Querier(ctx), builder)
  if err != nil {
    return nil, err
  }
  return model, nil
}

func (s *Storage) Delete{{$modelName}}(ctx context.Context, params models.Delete{{$modelName}}Params) (*models.{{.ModelName}}, error) {
  builder := sql.NewDelete().
    From(string(models.{{$modelName}}_TableName)).
    {{- range .ModelFields}}
    {{- if eq .FieldBadge "pk"}}
    Where(sq.Eq{string(models.{{.FieldName}}_{{$modelName}}_Field): params.{{.FieldName}}}).
    {{- end}}
    {{- end}}
    Suffix(models.SuffixReturning)

  model, err := pg.Get[*models.{{$modelName}}](ctx, s.executor.Querier(ctx), builder)
  if err != nil {
    return nil, err
  }
  return model, nil
}
