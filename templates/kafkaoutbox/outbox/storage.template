// Code generated by Boiler; DO NOT EDIT.
package kafkaoutbox

import (
  {{- range .OutboxStoragePackages}}
  {{.ImportAlias}} "{{.ImportLine}}"
  {{- end}}
)

type storage struct {
  executor  Executor
  lockTTL   time.Duration
  batchSize uint32
}

type Executor interface {
	Querier(ctx context.Context) pg.Querier
	Execer(ctx context.Context) pg.Execer
}

func NewStorage(executor Executor, lockTTL time.Duration, batchSize uint32) Storage {
  return &storage{
    executor:  executor,
    lockTTL:   lockTTL,
    batchSize: batchSize,
  }
}

func (s *storage) LockRecords(ctx context.Context, tableName string) ([]*Record, error) {
  query := `update %s set locked_until = now() + interval '%d millisecond' where id in (
                select id from %[1]s where locked_until is null or locked_until < now()
                order by created_at desc limit %d for update skip locked
            ) returning *;`

  query = fmt.Sprintf(query, tableName, s.lockTTL.Milliseconds(), s.batchSize)

  records, err := pg.Select[*Record](ctx, s.executor.Querier(ctx), sq.Expr(query))
  if err != nil {
    return nil, fmt.Errorf("pg.SelectCtx: %w", err)
  }
  return records, nil
}

func (s *storage) DeleteRecord(ctx context.Context, tableName, recordID string) error {
  query := fmt.Sprintf(`delete from %s where id = '%s';`, tableName, recordID)
  return pg.Exec(ctx, s.executor.Execer(ctx), sq.Expr(query))
}
