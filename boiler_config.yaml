# Config generated by Boiler; YOU MAY CHANGE THIS.
version: "1"

# Boiler app section
app:
  name: "app"
  version: "1.0.0"
  description: "description"

# Custom keys section
custom:
  pg_conn_timeout:
    group: "pg_conn"
    type: "duration"
    value: "3s"
    description: "description"

  kafka_errors_per_topic:
    group: "kafka"
    type: "string"
    value: "value"
    description: "description"

